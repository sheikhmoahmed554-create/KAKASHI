# KAKASHI SQZMOM HTF — إشارات شراء/بيع من Squeeze Momentum

مؤشر Pine v6 مبني على **Squeeze Momentum Indicator [LazyBear]**، بيحوّل تحوّل
الهيستوجرام حول خط الصفر لإشارات شراء/بيع، مع إدارة صفقة كاملة (هدف/ستوب/جدول)
بنفس شكل وألوان نسخة KAKASHI الأصلية.

الملف: `KAKASHI_SQZMOM_HTF.pine`

---

## القاعدة الأساسية

* الهيستوجرام **تحت الصفر (أحمر)** وبيتحوّل **فوق الصفر (أخضر)** → **شراء**.
* الهيستوجرام **فوق الصفر (أخضر)** وبيتحوّل **تحت الصفر (أحمر)** → **بيع**.

## الشروط الأربعة المطلوبة (مبنية بالكود)

| # | الشرط | الإعداد |
|---|-------|---------|
| ١ | الهدف والستوب إنت بتكتبهم، وبينرسموا عالشارت | `Target Points` / `Stop Points` + خطوط ولايبلات |
| ٢ | المؤشر بيقرا **فريم تاني** غير فريم الشارت | `فريم المؤشر (Signal Timeframe)` |
| ٣ | الصفقة ما بتفتح إلا **بعد إغلاق شمعة الفريم التاني** — بدون ريبينت | `request.security` بـ `lookahead_off` + قفل `barstate.isrealtime` |
| ٤ | **عدد شموع التأكيد** إنت بتختاره | `عدد شموع التأكيد` |

### كيف بيشتغل الفريم التاني بالضبط

إنت فاتح شارت **الدقيقة**، والمؤشر بيحسب Squeeze Momentum على الفريم اللي اخترته
(مثلاً M5 أو M15). قيمة الفريم الكبير بتوصل لشارت الدقيقة **فقط بعد ما تسكّر
شمعته**، وبعدين بينفتح الدخول على إغلاق شمعة الدقيقة اللي وصلت فيها القيمة.

يعني على M5: شمعة 04:10–04:15 بتسكّر الساعة 04:15، والدخول بيصير عند إغلاق شمعة
الدقيقة اللي بتسكّر بنفس اللحظة (أو اللي بعدها مباشرة حسب المنصّة). ما في ولا حالة
بيتغيّر فيها سهم بعد ما يطلع — لأنه محسوب على شمعة مسكّرة.

**عدد شموع التأكيد** = كم شمعة من الفريم الكبير لازم يظل الزخم بنفس اللون قبل ما
تطلع الإشارة. `1` يعني لحظة التحوّل نفسها، `3` يعني بعد ٣ شموع خضراء متتالية.

---

## الإعدادات

### ① الفريم والتأكيد
* **فريم المؤشر** — `1,2,3,5,15,30,60,120,240,D`
* **عدد شموع التأكيد** — من ١ لـ ٥٠
* **نوع الإشارة**
  * `Zero Cross` (الافتراضي) — عبور خط الصفر: أحمر → أخضر
  * `Early Turn` — دخول مبكر: الزخم يبدأ يرتد وهو لسا تحت الصفر (لون المارون عند LazyBear)
* **قفل ضد الريبينت** — بالوقت الحقيقي بياخد آخر شمعة مسكّرة بس

### ② إعدادات Squeeze Momentum
نفس إعدادات LazyBear (BB 20/2.0، KC 20/1.5، TrueRange).

> **مهم — مطابقة LazyBear:** بالكود الأصلي في سطر بيضرب انحراف البولنجر بـ
> `multKC` (1.5) بدل `mult` (2.0)، يعني خانة "BB MultFactor" فعلياً بلا تأثير.
> الخيار `مطابقة LazyBear الأصلية` مفعّل افتراضياً حتى تطلع نفس أرقام المؤشر
> المنشور على TradingView. إطفاؤه بيستخدم `BB MultFactor` صح — بس النتيجة رح
> تختلف عن الشارت اللي إنت شايفه.

### ③ الهدف والستوب
`1 Point = 0.10` (ذهب)، الهدف والستوب بالنقاط، وقاعدة لما ينضرب الاتنين بنفس
الشمعة (`Skip` افتراضياً — نفس قاعدة محرك KAKASHI).

### ④ فلاتر اختيارية (كلها مطفية افتراضياً)
* الانضغاط مفكوك (SqzOff)
* الموجة السابقة حقيقية — قمة الزخم بالموجة المعاكسة قبل التحوّل ≥ حد معيّن
* موافقة ترند الفريم الكبير (EMA)
* حد أدنى لـ ADX
* ساعات التداول (UTC)
* تبريد بعد كل صفقة

### ⑤ العرض
إظهار الإشارات، خطوط الدخول/الهدف/الستوب، والجدول.

---

## الجدول

نفس تنسيق وألوان جدول KAKASHI (فوق يمين، عمودين): Win / Loss / WR / Net /
Max W / Max L / Avg TP / Avg SL / Last / From، وزيادة عليها صفوف خاصة بالمؤشر:
Signal TF، Confirm (الجاري / المطلوب)، Momentum + لونه، Squeeze + قمة الموجة،
TP/SL، والصفقة المفتوحة.

---

## التشغيل داخل KAKASHI PineLab

الملف بيعرّف نفس أسماء المتغيّرات اللي بتقراها نسخك:

* `canEnterLong` / `canEnterShort` — للنسخة اللي بتحسب الباك تست بالمحرك
  (`index.html`).
* `sy348EntryBuyPlot` / `sy348EntrySellPlot` / `sy348ExitTPPlot` /
  `sy348ExitSLPlot` / `entryPrice` / `tpPrice` / `slPrice` / `tradeTargetPts` /
  `tradeStopPts` / `wins` / `losses` / `trades` / `netPts` / `maxWin` /
  `maxLoss` / `statsActive` … — للنسخ اللي بتقرا حالة الصفقة من داخل المؤشر
  (`KAKASHI_V16_TV_PARITY_AUDIT.html`).

يعني بتلصقه بمربع الكود وبتضغط تشغيل زي أي نسخة تانية.

---

## التحقق من عدم الريبينت

انختبر عملياً: انقطعت الداتا عند ٨ نقاط مختلفة وانعاد التشغيل، وانقارنت
الإشارات مع التشغيل الكامل. **كل الإشارات طلعت متطابقة ١٠٠٪** — يعني الإشارة
لما بتطلع ما بتتغيّر ولا بتختفي.

## نتائج الاختبار على داتا 2026 المدمجة (XAUUSD M1)

اختبار مباشر بمحرك KAKASHI نفسه، هدف وستوب **متساويين ٥٠ نقطة** عشان نسبة
الربح تعكس جودة الإشارة لحالها (فوق ٥٠٪ = في ميزة):

| الإعداد | يناير–مارس (60 ألف) | مارس–يوليو (132 ألف) |
|---------|:---:|:---:|
| M5 خام | 50.7% (608) | 45.6% (1281) |
| M5 + موجة ≥ 60 | 53.2% (436) | 47.1% (851) |
| M15 خام | 50.8% (197) | 47.7% (434) |
| M15 + موجة ≥ 60 | 50.0% (154) | 48.7% (343) |
| M15 + موجة ≥ 100 + confirm 2 | 43.7% (135) | 47.1% (289) |
| M15 + ترند EMA50 | 54.8% (126) | 46.7% (259) |
| M30 + موجة ≥ 150 | 34.8% (66) | 54.0% (150) |
| **M60 + موجة ≥ 200** | **53.8% (26)** | **53.2% (79)** |

(الرقم بين قوسين = عدد الصفقات)

**الخلاصة الصادقة:**

* الإشارة الخام (عبور الصفر لحاله) نتيجتها حوالي ٥٠٪ — يعني بلا ميزة.
* أغلب الفلاتر اللي طلعت ممتازة بالنصف الأول انقلبت بالنصف التاني
  (ترند EMA50: ‏54.8% → 46.7%، و M30: ‏34.8% → 54.0%). هذا اسمه ضجيج مو ميزة.
* الإعداد الوحيد اللي ظل فوق ٥٠٪ **بالفترتين** هو **M60 + فلتر الموجة ≥ 200**،
  بس بـ ١٠٥ صفقة إجمالاً — عدد صغير ما بيثبت شي.
* بإعداداتك الافتراضية (هدف ٥٠ / ستوب ٦٥) الستوب أكبر من الهدف، فلازم نسبة ربح
  أعلى من ٥٦.٥٪ حتى تتعادل.

الكود صحيح ومختبَر على المحركين، بس لا تعتمد على أي إعداد قبل ما تجرّبه بنفسك
على فترتين مختلفتين على الأقل.

## ملاحظة توافق

قاعدة "الهدف والستوب انضربوا بنفس الشمعة = Skip" مطبّقة زي ما هي بكود Pine
تبعك: الصفقة **بتتسكّر** وما بتنعد بالإحصائيات. (محرك الباك تست بالبايثون
داخل `index.html` بيتعامل معها بشكل مختلف — بيتخطّى الشمعة وبيخلّي الصفقة
مفتوحة. الفرق موجود أصلاً بنظامك، مو من هذا الملف.)

---

الأصل: Squeeze Momentum Indicator [LazyBear] — Mozilla Public License 2.0
